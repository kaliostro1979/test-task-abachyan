<div class="container">
    <div class="page">
        <h1 class="page__title">{{ page.title }}</h1>
        <time class="page__time">{{ page.published_at | date: "%b %d, %Y, %H:%m%p"  }}</time>
        <div class="page_main">
            <div class="page-content">
                {{ page.content }}
            </div>

        </div>
    </div>

</div>